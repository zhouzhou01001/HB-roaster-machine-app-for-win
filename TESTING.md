# 周周 ROAST 测试与发布验收

## 自动化测试

```powershell
python -m pytest -q
python scripts/run_release_tests.py
```

当前源码版本为 `1.0`。测试结论以本次生成的 `release/evidence/test-report-1.0.json` 为准。pytest 同时收集 TestCase 和顶层测试函数；旧版 unittest 的通过数量不代表全部测试。历史 EXE 冒烟记录不能证明新源码构建的 EXE。

覆盖范围：

- `开始采集 -> 开始烘焙 -> 入豆 -> 排豆 -> 归档` 状态机。
- 入豆前数据不写入正式锅次，入豆后计时从 `00:00` 开始。
- 重复点击、非法状态、断线重连和采集线程回归。
- HB 帧解析、来源审计、通道映射、模拟模式显式选择。
- RoR、回温点、发展期、里程碑预测和样本质量门。
- 事件记录/取消/刷新、人工参数和 DROP 锁定。
- SQLite 原子归档、旧数据兼容、回放、对比和参考曲线。
- 统一曲线、双纵轴、非负轴、滚轮禁用、自动跟随和 PDF。
- 720P 到 4K 响应式布局与右上温度/RoR 字符完整显示。
- 调试日志、诊断包、工作目录回退和导出确认门。

## 发布硬门槛

1. 自动化测试全部通过。
2. 从 `app/version.py` 生成 Windows 版本资源，依赖与锁定文件一致。
3. `python scripts/build_exe.py --smoke-exe` 返回退出码 `0`。
4. EXE 的文件版本、产品版本和文件名一致。
5. EXE 至少完成 10 秒启动冒烟测试且未提前退出。
6. 计算并保存 SHA-256。
7. 交付 ZIP 通过 CRC 完整性检查。

## 真机人工验收

1. 拔插 USB 后确认对应 COM 口同步消失和出现。
2. 点击开始采集，确认四路温度持续刷新，且没有正式烘焙计时。
3. 点击开始烘焙，确认显示预热曲线并提示等待入豆。
4. 点击入豆，确认计时从 `00:00` 开始，正式曲线不含入豆前数据。
5. 核对豆温、环境/炉温、进风温、排风温与机身面板读数。
6. 确认进风/排风 RoR 和豆温 RoR 均为整数显示且字符完整。
7. 确认排风温曲线存在，曲线纵轴永不出现负刻度。
8. 滚轮不改变坐标范围，左键拖动仍可查看曲线。
9. 记录转黄、一爆、二爆和排豆，确认时间均以入豆为零点。
10. 排豆后确认正式记录停止而实时温度仍继续显示。
11. 连续完成两锅，确认上一锅数据、事件和计时不污染下一锅。
12. 烘焙中断开并恢复设备，确认缺失区间不补造 `0°C` 数据。
13. 导出 CSV/JSON/PDF，确认写入前预览和文件内容。
14. 在 `1280x720` 与 `1920x1080` 实机检查按钮、LCD 和曲线无裁切。

## 已知测试边界

- 自动化测试使用模拟串口和离屏 Qt，不能替代长时间真机稳定性测试。
- 当前自动化验证保持既有 Model S 物理重排；实际通道对应、固件确认响应和长时间稳定性仍须真机验收。
- PyInstaller 可选组件警告不影响当前二维曲线，但应在后续最小化构建时消除。

